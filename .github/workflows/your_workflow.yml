# Let's update the workflow file as per the new changes and prepare it for commit and push

workflow_content = """
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Check out the repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.12'

    - name: Install Python build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install python3-distutils

    - name: Install pip
      run: |
        curl -sS https://bootstrap.pypa.io/get-pip.py | python

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: pytest
"""

# Write the updated workflow content to the file
workflow_path = '/mnt/data/your_workflow.yml'
with open(workflow_path, 'w') as file:
    file.write(workflow_content)

workflow_path
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Check out the repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.12'

    - name: Install Python build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install python3-distutils

    - name: Install pip
      run: |
        curl -sS https://bootstrap.pypa.io/get-pip.py | python

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: pytest

